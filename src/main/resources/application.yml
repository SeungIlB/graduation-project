server:
  address: localhost
  port: 8080

spring:
  redis:
    host: localhost # 로컬에서 테스트 할 때는 localhost로 사용
    port: 6379
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/GraduationProject
    username: webjunior
    password: niceweb

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: 1776f50389f29f504dcee37aec880b889a6fad8c2dd2c508fd9e2a989af9f52d
  accessTokenExpirationMs: 86400000 # 24시간
  refreshTokenExpirationMs: 604800000 # 7일

